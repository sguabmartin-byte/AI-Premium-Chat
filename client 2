
import { NextResponse } from "next/server";

export async function POST(req: Request) {
  const { message } = await req.json();

  // Just nu: fejk-svar så vi kan testa
  return NextResponse.json({ reply: "Hej! Detta är ett testsvar från AI 🤖" });
}
